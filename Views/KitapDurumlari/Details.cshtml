@model KitaplikApp.Models.KitapDurumlari

@{
    ViewData["Title"] = "Kitap Durumu Detayları";
}

<h1>Kitap Durumu Detayları</h1>

<div>
    <h4>Kitap Durumu</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.DurumAdi)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.DurumAdi)
        </dd>
    </dl>
</div>
<div class="mt-3">
    <a asp-action="Upsert" asp-route-id="@Model?.DurumId" class="btn btn-warning me-2">Düzenle</a>
    <a asp-action="Index" class="btn btn-secondary">Listeye Geri Dön</a>
</div>